{
  "name": "rcu",
  "version": "0.4.0-edge",
  "description": "Utilities for implementing Ractive.js component loaders",
  "license": "MIT",
  "repository": "https://github.com/ractivejs/rcu",
  "dependencies": {
    "eval2": "^0.3.0",
    "vlq": "^0.2.0"
  },
  "devDependencies": {
    "esperanto": "^0.6.11",
    "eval2": "^0.2.0",
    "ractive": "git://github.com/ractivejs/ractive#build",
    "spelunk": "^0.1.1",
    "vlq": "^0.1.0"
  },
  "main": "dist/rcu.js",
  "jsnext:main": "src/rcu.js",
  "scripts": {
    "build": "esperanto -b -d src -i rcu.js -o dist/rcu.js -t umd -n rcu",
    "test": "node test",
    "prepublish": "npm run build; npm test"
  }
}
